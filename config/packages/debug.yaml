when@dev:
    debug:
        # dump_destination disabled - it tries to TCP-connect to a VarDumper server
        # on every request, causing timeouts if the server isn't running.
        # To re-enable: run "php bin/console server:dump" first, then uncomment below.
        # dump_destination: "tcp://%env(VAR_DUMPER_SERVER)%"
